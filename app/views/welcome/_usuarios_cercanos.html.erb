<ul>
  <h2><%= current_user.address %></h2>
  <% nearbys.each do |near| %>
    <% unless near.nil? %>
      <li><%= near.email %> - <%= (current_user.distance_from([near.latitude, near.longitude]) * 1.6) %> km. - <%= near.address %></li>
    <% end %>
  <% end %>
</ul>
